{
  "name": "ldwg-me",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules build && npm install",
    "start": "wrangler pages dev build",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-webpack": "^3.0.0",
    "prettier": "^2.4.1",
    "prop-types": "^15.7.2",
    "snowpack": "^3.8.8",
    "wrangler": "^2.0.22"
  },
  "dependencies": {
    "98.css": "^0.1.17",
    "classnames": "^2.3.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "uuid": "^8.3.2"
  }
}
